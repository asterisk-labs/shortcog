"""shortcog. A COG profile for AI training data."""

__version__ = "0.1.0"

# C++ bindings will be exposed here once the nanobind module is built.
# from ._core import read, read_batch, info, index_file, validate
